<form method="post" action="{{ path('admin_plane_delete', {'id': plane.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
  <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plane.id) }}">
  <button class="btn btn-outline-danger">Delete</button>
</form>