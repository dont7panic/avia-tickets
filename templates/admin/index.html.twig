{% extends 'base.html.twig' %}

{% block title %}Hello Controller!{% endblock %}

{% block body %}
  <div class="example-wrapper">
    <h1>Hello, ! ✅</h1>

    <ul>
      <h2>Choose your entity</h2>
      {% for key, controller in controllers %}
        <li><a href="{{ path(controller) }}">{{ key }}</a></li>
      {% endfor %}
    </ul>
  </div>
{% endblock %}